namespace: monitoring
generatorOptions:
  disableNameSuffixHash: true
commonAnnotations:
  prometheus.io/config: "true"
configMapGenerator:
- name: kube-apiserver-prometheus
  files:
  - prometheus.yml
