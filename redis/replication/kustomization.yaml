commonLabels:
  app: redis
  comp: server
configMapGenerator:
- name: redis
  files:
  - redis.conf
secretGenerator:
- name: redis
  literals:
  - REDIS_PASSWORD=secret
  - SENTINEL_USER=sentinel
  - SENTINEL_PASSWORD=U6FP4PBA0r
resources:
- service.yaml
- statefulset.yaml
