commonLabels:
  app: redis
  comp: server
configMapGenerator:
- name: redis
  files:
  - redis.conf
secretGenerator:
- name: redis
  literals:
  - ROOT_USER=root
  - ROOT_PASSWORD=secret
resources:
- service.yaml
- statefulset.yaml
