commonLabels:
  app: cassandra
configMapGenerator:
- name: cassandra
  files:
  - cassandra.yaml
  - cassandra-rackdc.properties
secretGenerator:
- name: cassandra
  literals:
  - CASSANDRA_PASSWORD=secret
resources:
- statefulset.yaml
- change-password.yaml
